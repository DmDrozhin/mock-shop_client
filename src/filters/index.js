import toAccFormat from '../filters/toAccFormat.js'
import toUpperCase from '../filters/toUpperCase.js'

export default [
  { name: 'toAccFormat', prop: toAccFormat },
  { name: 'toUpperCase', prop: toUpperCase }
]